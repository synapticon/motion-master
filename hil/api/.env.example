# URL of the Motion Master instance under test (no trailing slash).
# The test suite builds and starts a Docker container automatically.
# Override if you want to test against an already-running instance.
MM_URL=https://local.motion-master.synapticon.com:61447

# Set to 1 to skip Docker management and use an already-running instance.
# MM_SKIP_DOCKER=1
