---
- name: Install and configure system packages
  hosts: ci_machines
  become: true
  roles:
    - { role: dev-toolchain, tags: dev-toolchain }

- name: Set up GitHub Actions runner
  hosts: ci_machines
  become: false
  roles:
    - { role: actions-runner, tags: actions-runner }
