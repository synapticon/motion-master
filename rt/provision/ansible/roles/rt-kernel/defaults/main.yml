---
# Debian ships PREEMPT_RT in the archive, so there is nothing to build or patch:
# Forky carries linux-image-rt-{amd64,arm64} 7.1.3 in main, the same version on
# both architectures, which is what lets the AAeon board and the Raspberry Pi 5
# run an identical kernel.
rt_kernel_install_headers: true
